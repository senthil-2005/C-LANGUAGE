#include <stdio.h>
int main()     
{
    int num1, num2, add;
    printf("Enter two integers: ");
    scanf("%d %d", &num1, &num2);
    add = num1 + num2;      
    printf("%d + %d = %d", num1, num2, add);
    return 0;
}
